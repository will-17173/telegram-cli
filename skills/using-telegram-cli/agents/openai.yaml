interface:
  display_name: "Using Telegram CLI"
  short_description: "Install and operate Telegram CLI safely"
  default_prompt: "Use $using-telegram-cli to ensure tg is installed, then run safe Telegram account, message, contact, folder, notification, archive, or group operations."
